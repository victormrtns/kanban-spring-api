version: '3'

services:
  #  app:
  #    build:
  #      context: .
  #      dockerfile: DockerFile
  #    ports:
  #      - "8080:8080"
  #    depends_on:
  #      - postgres
  #    container_name: kanban-backend-container
  postgres:
    image: postgres:13.1
    environment:
      POSTGRES_DB: kanban
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: senha
    ports:
      - "5432:5432"
    container_name: kanban-db-container